{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "",
  "license": "MIT",
  "author": "John Okorie",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch --inspect index.js ",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../part2/phonebook && yarn build && cp -r dist ../../part3/phonebook_backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m 'Deploy Phonebook App' && git push"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.11.0"
  }
}
